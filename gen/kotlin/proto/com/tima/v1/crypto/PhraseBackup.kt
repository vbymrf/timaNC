// Code generated by Wire protocol buffer compiler, do not edit.
// Source: tima.v1.crypto.PhraseBackup in tima/v1/crypto/key_delivery.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package com.tima.v1.crypto

import com.squareup.wire.FieldEncoding
import com.squareup.wire.Message
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.immutableCopyOf
import com.squareup.wire.`internal`.redactElements
import com.tima.v1.common.Uuid
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import kotlin.collections.List
import okio.ByteString

public class PhraseBackup(
  @field:WireField(
    tag = 1,
    adapter = "com.tima.v1.common.Uuid#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "ownerId",
    schemaIndex = 0,
  )
  public val owner_id: Uuid? = null,
  @field:WireField(
    tag = 2,
    adapter = "com.squareup.wire.ProtoAdapter#UINT32",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "backupVersion",
    schemaIndex = 1,
  )
  public val backup_version: Int = 0,
  keys: List<PhraseBackupKey> = emptyList(),
  @field:WireField(
    tag = 4,
    adapter = "com.squareup.wire.ProtoAdapter#BYTES",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 3,
  )
  public val signature: ByteString = ByteString.EMPTY,
  unknownFields: ByteString = ByteString.EMPTY,
) : Message<PhraseBackup, Nothing>(ADAPTER, unknownFields) {
  @field:WireField(
    tag = 3,
    adapter = "com.tima.v1.crypto.PhraseBackupKey#ADAPTER",
    label = WireField.Label.REPEATED,
    schemaIndex = 2,
  )
  public val keys: List<PhraseBackupKey> = immutableCopyOf("keys", keys)

  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is PhraseBackup) return false
    if (unknownFields != other.unknownFields) return false
    if (owner_id != other.owner_id) return false
    if (backup_version != other.backup_version) return false
    if (keys != other.keys) return false
    if (signature != other.signature) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + (owner_id?.hashCode() ?: 0)
      result = result * 37 + backup_version.hashCode()
      result = result * 37 + keys.hashCode()
      result = result * 37 + signature.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    if (owner_id != null) result += """owner_id=$owner_id"""
    result += """backup_version=$backup_version"""
    if (keys.isNotEmpty()) result += """keys=$keys"""
    result += """signature=$signature"""
    return result.joinToString(prefix = "PhraseBackup{", separator = ", ", postfix = "}")
  }

  public fun copy(
    owner_id: Uuid? = this.owner_id,
    backup_version: Int = this.backup_version,
    keys: List<PhraseBackupKey> = this.keys,
    signature: ByteString = this.signature,
    unknownFields: ByteString = this.unknownFields,
  ): PhraseBackup = PhraseBackup(owner_id, backup_version, keys, signature, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<PhraseBackup> = object : ProtoAdapter<PhraseBackup>(
      FieldEncoding.LENGTH_DELIMITED, 
      PhraseBackup::class, 
      "type.googleapis.com/tima.v1.crypto.PhraseBackup", 
      PROTO_3, 
      null, 
      "tima/v1/crypto/key_delivery.proto"
    ) {
      override fun encodedSize(`value`: PhraseBackup): Int {
        var size = value.unknownFields.size
        if (value.owner_id != null) {
          size += Uuid.ADAPTER.encodedSizeWithTag(1, value.owner_id)
        }
        if (value.backup_version != 0) {
          size += ProtoAdapter.UINT32.encodedSizeWithTag(2, value.backup_version)
        }
        size += PhraseBackupKey.ADAPTER.asRepeated().encodedSizeWithTag(3, value.keys)
        if (value.signature != okio.ByteString.EMPTY) {
          size += ProtoAdapter.BYTES.encodedSizeWithTag(4, value.signature)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: PhraseBackup) {
        if (value.owner_id != null) {
          Uuid.ADAPTER.encodeWithTag(writer, 1, value.owner_id)
        }
        if (value.backup_version != 0) {
          ProtoAdapter.UINT32.encodeWithTag(writer, 2, value.backup_version)
        }
        PhraseBackupKey.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.keys)
        if (value.signature != okio.ByteString.EMPTY) {
          ProtoAdapter.BYTES.encodeWithTag(writer, 4, value.signature)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: PhraseBackup) {
        writer.writeBytes(value.unknownFields)
        if (value.signature != okio.ByteString.EMPTY) {
          ProtoAdapter.BYTES.encodeWithTag(writer, 4, value.signature)
        }
        PhraseBackupKey.ADAPTER.asRepeated().encodeWithTag(writer, 3, value.keys)
        if (value.backup_version != 0) {
          ProtoAdapter.UINT32.encodeWithTag(writer, 2, value.backup_version)
        }
        if (value.owner_id != null) {
          Uuid.ADAPTER.encodeWithTag(writer, 1, value.owner_id)
        }
      }

      override fun decode(reader: ProtoReader): PhraseBackup {
        var owner_id: Uuid? = null
        var backup_version: Int = 0
        val keys = mutableListOf<PhraseBackupKey>()
        var signature: ByteString = ByteString.EMPTY
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> owner_id = Uuid.ADAPTER.decode(reader)
            2 -> backup_version = ProtoAdapter.UINT32.decode(reader)
            3 -> keys.add(PhraseBackupKey.ADAPTER.decode(reader))
            4 -> signature = ProtoAdapter.BYTES.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return PhraseBackup(
          owner_id = owner_id,
          backup_version = backup_version,
          keys = keys,
          signature = signature,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: PhraseBackup): PhraseBackup = value.copy(
        owner_id = value.owner_id?.let(Uuid.ADAPTER::redact),
        keys = value.keys.redactElements(PhraseBackupKey.ADAPTER),
        unknownFields = ByteString.EMPTY
      )
    }

    private const val serialVersionUID: Long = 0L
  }
}
